# Demo plugin

The `demo` plugin exists for testing purposes. It allows you to test out the UI without any hardware capture
device connected, and will generate a few simple signals.

The plugin is bundled with OpenMSO to enable quick evaluation by new users, and to allow developers to demonstrate
user interface changes in a hardware-agnostic manner.

