[package]
name = "demo"
version = "0.0.1"
description = "OpenMSO capture plugin: simulated mixed-signal device (no hardware needed)"
edition.workspace = true
license.workspace = true

[dependencies]
openmso.workspace = true
serde_json.workspace = true
